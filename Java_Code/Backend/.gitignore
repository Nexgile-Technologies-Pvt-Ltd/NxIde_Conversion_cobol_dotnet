target/
!.mvn/wrapper/maven-wrapper.jar

### IDE ###
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

### Local overrides ###
# .env is committed deliberately so the team shares one working configuration.
# Per-developer overrides go in .env.local, which is not.
.env.local
application-local.yml
application-local.properties
*.log
